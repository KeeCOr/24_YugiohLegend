# YugiohLegend 기획서 v0.4.6

> **최종 업데이트:** 2026-06-24

---

## 문제 정의

**어떤 사람이:** 유희왕·매직·개더링 같은 카드 배틀 게임을 좋아하지만 방대한 룰과 긴 진행 시간 문제에 캐주얼하게 즐기기 어려운 사람.

**어떤 상황에서:** 출퇴근·점심 시간 등 10분 이외의 짧은 여유 시간에 빠르게 한 판을 마칠 수 있는 복잡한 규칙을 몰라도 현재 상황에서 최선의 수를 직관으로 파악하고 싶은 상황.

**Pain (핵심 고통):** 복잡한 콤보나 전략의 깊이를 즐기려 해도 현재 가능한 선택지가 한눈에 들어오지 않고 즉각적인 결과 피드백이 없어, 짧은 세션에서는 충분한 전략적 만족감이 충족되지 않아 이탈한다.

---

## 주 페르소나

**김지윤 (29세) IT 스타트업 백엔드 개발자**

- **현재 게임 경험:** 전에는 유희왕 카드를 즐겼으나, 사회에 나온 이후 실제 카드 게임은 시간·비용 문제로 포기. 현재는 퇴근 후 30분 이외로 모바일 퍼즐·전략 게임을 가끔 즐김. 긴 튜토리얼이 있으면 바로 튀어나오는 것.
- **현재 행동(대안 게임):** 마스터 듀얼을 플레이하지만 조작 피드백이 부족해 졌을 때 아쉬움이 남는다. 결국 화면에서 다음 행동의 개선 힌트와 즉각 피드백을 원하지만 해소되지 않아 다음 판을 시작하지 못한다.
- **이 게임을 플레이하는 맥락:** 지하철 출퇴근 이어폰 꽂고 한 판씩. 규칙 설명 없이 화면부터 바로 선택 가능한 상황을 보여주기를 원함. 지거나 이기더라도 "왜 그랬지 좋았는데/아쉬웠는데"가 바로 이해되어야 다음 판을 시작함.
- **특이점:** 코딩하는 직업 특성상 로직 파악이 빠름. 전략적인 게임에서도 UI가 정보를 알아볼 수 있게 구조화해주기를 기대함. "룰북 읽기" 없이 3판 이내에 게임 이해가 목표.

---

## 핵심 루프

```
플레이어가 현재 카드 상황(LP·레인·손패)을 확인
  → 이번 턴 → 몬스터/마법/함정 배치 결정
  → 행동 커밋 → 동시 공개 → 레인별 전투 자동 처리
  → LP 갱신·결과 피드백 확인
  → 보상으로 강화된 덱원으로 다음 턴 준비
  → 다시 "현재 카드 상황 확인"으로 돌아감
```

**패배 이후 메타 루프:**

```
전적 결과 확인
  → 덱 빌더에서 카드 조합 조정
  → 다음 판 시작
  → 다시 핵심 루프
```

---

## MVP 가설
| # | 핵심 기능 | 검증할 가설 | 검증 방법 |
|---|-----------|-------------|-----------|
| 1 | 4레인 동시 공개 배틀 | 비공개 입력·동시 공개 구조가 있으면 플레이어가 첫 판에도 "전략을 쓰는 느낌"을 경험하고 발산적으로 시도할 것이다 | 1판 플레이 후 시도율 60% 이상 |
| 2 | 위험/보상 시각화 | 현재 레인별 위험성과 가능한 이득을 시각으로 표시하면 주요 선택 평균 시간이 8분 이내로 줄고 결과 불만 피드백이 20% 이하가 될 것이다 | 선택 시간 측정, 결과 화면 피드백 집계 |
| 3 | 지속 마법/함정 시스템 | 마법·함정의 지속 발동 구조가 있으면 플레이어가 3판 이내에 스스로 새로운 콤보 빌드를 발산적으로 시도할 것이다 | 3판 이후 덱 변경률 50% 이상 |
| 4 | 레인 금화 진행 구조 | 각 레인에서 마지막 레인에 금화를 배치하는 구조가 있으면 최후의 판까지 3레인까지 큰 이탈 없이 진행할 것이다 | 최후 3레인 달성률 70% 이상 |
| 5 | 솔로 AI 전투 | AI와의 솔로 플레이가 가능하면 실시간 매칭 없이도 즉시 세션을 시작하는 플레이어가 70% 이상일 것이다 | 첫 세션 AI 전투 선택률 측정 |

---

## 레퍼런스 분석

### 1. 유희왕 마스터 듀얼 (모바일·PC)
- **핵심 행동까지 UX 단계 수:** 설치 → 계정 생성 → 튜토리얼(약 15분) → 첫 상대 전투 = **6~8단계, 20분 이상**
- **강점:** 원작 룰 완벽 재현, 풍부한 카드 풀
- **단점:** 진입 장벽 높음, 한 당 시간이 길어 모바일 세션에 부적합
- **적용 교훈:** 룰 설명보다 먼저 선택 가능한 상황을 보여주기를 시작, 복잡성 분산 위해 레인 1개만 줄여 복잡성을 분산시킴(YugiohLegend의 레인 금화 구조가 이 교훈을 직접 적용).

### 2. Clash Royale (모바일)
- **핵심 행동까지 UX 단계 수:** 앱 실행 → 튜토리얼 배틀 시작 = **2단계, 90초 이내**
- **강점:** 즉시 플레이 가능, 짧은 세션(3분), 전략적 전략
- **단점:** 카드 집결 P2W 구조, 덱 빌딩 깊이 부족
- **적용 교훈:** 첫 플레이까지 단계를 최소화 목표: 앱 실행 후 2단계, 30초 이내 첫 선택). 결과 화면에서 다음 판의 개선 힌트를 즉시 제안.

### 3. Slay the Spire (PC/모바일)
- **핵심 행동까지 UX 단계 수:** 실행 → 캐릭터 선택 → 첫 전투 = **3단계, 2분 이내**
- **강점:** 덱 빌딩 심화성, 극명한 선택지와 전략이 명확
- **단점:** 단일 플레이어, 경쟁 요소 없음
- **적용 교훈:** 카드 효과를 "발동 조건 / 효과 결과" 2줄 구조로 단순화해 보는 서식을 명확화(v0.4.6에 직접 반영).

---

## 성공 KPI

| 지표 | 목표값 |
|------|--------|
| 첫 세션 평균 플레이 시간 | 8분 이상 |
| 첫 세션 후 2차 진입률 | 55% 이상 |
| 최후 3레인 달성률 | 70% 이상 |
| 주요 선택 화면 평균 결정 시간 | 8분 이내 |
| 핵심 선택 화면 이탈률/무응답 | 15% 이하 |
| 3판 이후 덱 변경 시도율 | 50% 이상 |
| D1 리텐션 | 30% 이상 |
| 결과 화면 "이해 불가" 피드백 비율 | 20% 이하 |

---

## 게임 개요

YugiohLegend는 유희왕을 단순화한 **4레인 카드 전투 게임**이다. 양쪽 플레이어가 비공개로 행동을 입력하고 동시에 공개하면 서버가 전투를 자동 처리한다. 매 번의 선택은 다음 화면에서의 위험성·보상·성취 방향으로 이어지는 구조를 중심으로 한다.

- **스택:** Phaser 3 클라이언트 + Node.js WebSocket 서버 + Electron 래퍼
- **화면 기준 해상도:** 900 × 1600 (세로형)
- **현재 버전:** 0.4.6

---

## 게임 시스템
### 기본 설정

| 항목 | 값 |
|------|----|
| 시작 LP | 4000 |
| 기준 해상도 | 900 × 1600 |
| 덱 구성 | 4턴 + 상이한 조합·이용 |
| 1턴 기본 | 공격 또는 방어 중 하나 |
| 레인 금화 | 1턴 1금화, 2턴 2금화, 3턴 3금화, 4턴 4금화 |
| 시작 손패 | 4장 |
| 마지막 스페어 | 1장 |
| 마지막 몬스터 | 1장 |

### 레인 금화

- 1턴: 기본 레인만 사용
- 2턴: 금화 + 기본 레인
- 3턴: 3개의 레인
- 4턴: 4개의 레인 모두 활용
- 마지막 레인에서는 몬스터·마법 배치·함정 발동 서버가 처리
- 클라이언트는 마지막 레인의 자동적인 전투 결과를 활용해 결과를 표시

### 턴 흐름

**1턴(방어):**
1. 기본 레인에서 행동 입력
2. 몬스터 몬스터, 마법 배치, 함정 발동
3. 행동 공개
4. 전투 없이 카드/LP 상태만 갱신함
**2~4턴:**
1. 기존 지속 마법 이어받는 것을 갱신
2. 각쪽 행동 공개
3. 금화의 레인에서의 행동 적용
4. 레인별 전투 처리 결과
5. LP 갱신 및 0 이하가 되면 최종 결과

### 지속 마법 & 함정

- 마법은 설치하는 곳까지의 레인에 효과 발동
- 각 상대 마법·함정의 지속 발동 효과가 쌓이면서 표시
- `LaneState.spell = { card, remainingTurns }` 구조로 구현
- 턴 행동 적용 후 기존 마법의 `remainingTurns` 감소; 0이 되면 효과 발동 후 삭제
- 발동 후 해당 레인에 직접 공격하면 마법 무효화, 효과 역전

### 함정 시스템 (v0.4.1 추가)

- 조건부 발동 카드(`mirror_snare`, `direct_shield`, `overextend_collapse`)에 `trap` 타입
- 함정 선택지에서 뒤집기/공개 가능 여부를 표시하고 나머지 공개됨 face-up spell과 구분
- AI에서 함정을 뒤집을 경우의 경우 전투 전략을 달리 적용해 정보 인식

### 카드 효과 가독성 (v0.4.6)

- 카드 효과 텍스트를 `발동 조건` / `효과 결과` 2줄 구조로 분리
- 선택·취소 가능 텍스트에서의 동일 효과 발동 수의 사용 후 카드 선택 화면의 이펙트 표시, 현재 텍스트가 모두 선택 시 배치 제거
- 비ASCII 캐릭터로 선택된 빌드 분기 피드백을 위해 각 턴 버튼·이벤트 표시 캐릭터를 ASCII 기반으로 정리

---

## 화면 구조

- **게임 화면:** 각 측 카드 현황·전투 결과 카드·손패 (세로형 스택)
- **덱 빌더:** 카드 선택창 3턴 + 현재 덱 구성 목록

---

## 설계원칙과 UX 지침

- 주요 버튼 44px 이상, 화면당 CTA 강조색은 1개
- 버튼·선택지 매 화면에 5개 이하 (Hick's Law)
- 결과 피드백 400ms 이내 표시 (Doherty Threshold)
- 로딩·오류 상태·비움·장문 데이터 빈 상태 스켈레톤 UI도 준비
- HUD 동일 레이어 요소 최소 겹침 원칙; 겹침 필요 시 별도의 레이어도 depth/z-order 사용

---

## MVP 개발 현황 (2026-06-24 기준)

| 항목 | 완성도 |
|------|--------|
| 핵심 루프 구현 완료율 | 약 64% |
| 첫 세션 루프 작동 가능성 | 약 70% |
| UI/아이콘 정합성 | 약 66% |
| 레퍼런스·클라이언트 연동 | 약 60% |
| 빌드/테스트 안정성 | 약 74% |

---

## AI 생성 리소스 목록

| 파일명 | 생성 의도 |
|--------|-----------|
| art_backdrop.png | 게임 배경 배경 이미지 |
| art_button.png | 기본 UI 버튼 |
| art_buttonPrimary.png | 주요 행동(공격 후) 강조 버튼 |
| art_cardBack.png | 카드 뒷면 이미지 (비공개 상태) |
| art_cardMonster.png | 몬스터 카드 아트 |
| art_cardSpell.png | 마법 카드 아트 |
| art_glow.png | 카드 선택·활성화 발광 이펙트 |
| art_glow_tmp_alpha.png | 발광 이펙트 임시 알파 테스트 파일 |
| art_handRail.png | 손패 영역 경계 UI |
| art_hudFrame.png | HUD 프레임(LP·턴 표시 영역) |
| art_lane.png | 플레이어측 레인 배경 |
| art_laneEnemy.png | 상대방측 레인 배경 |
| art_laneFrame.png | 레인 경계 프레임 |
| art_panel.png | 범용 UI 목록 배경 |
| art_slash.png | 공격·전투 이펙트 이펙트 |

---

## 향후 로드맵과 다음 우선순위

1. 첫 화면에서 목표의 다음 행동이 5분 이내에 보이지 않으면 플레이어의 튜토리얼로 확인
2. 주요 선택의 결과를 예측하고 실제 결과가 달라지는 지점 파악
3. 매 턴 변경의 업데이트 순서로 공모 동작의 기획서 버전 확인

---

## 빌드 및 테스트
- **테스트:** `npm test`
- **빌드:** `npm run build:all && npx electron-builder --win portable`
- **현재 버전:** 0.4.6

---

## 개선 제안

> 아래 제안은 현재 기획서 논의를 기반으로 우선순위에 따라 개선 제안한다.

### 상위 게임·플레이 / 핵심 기능
| 우선순위 | 제안 | 기대 효과 |
|---------|------|---------|
| 높음 | 결과 화면에서 레인별 "왜 그랬는지 최선은 어땠는지?" 경험 힌트 표시 → 실제 전투 결과의 플레이어가 입력한 행동의 갈림수를 화살표로 표시 | 결과 이해 불가 피드백 20%를 0% 이하, D1 리텐션에 직접 기여 |
| 높음 | AI 스타일 3종류(공격/수비/균형) 적용 → 공격 AI는 함정을 너무 무시하지 않고, 균형 AI는 효율적인 마법 운용 → 이 점수 레인 분산 전략도 활용 | 최후 3레인 달성률 70% 달성, 솔로 플레이 AI 전투 솔직한 플레이 시간 확인 |
| 이후 | 상이한 조합과 이용 카드에 "몇 개 레인 이득 조건 적용 → 지속으로 LP 이이가 500 이하가 되면 1개의 레인에 2번의 공격을 하는 특수성의 선택지가 활성화 | 전체 가능성 확인으로 더욱 점 지점, 2차 진입률을 55%를 5% 목표 |
| 이후 | 손패에서 드래그·드롭으로 레인에 직접 배치하는 직관적인 인터페이스 적용 (현재 탭 선택 가로세로 대기 중) | 배치 화면에서 배치 시간 감소, 결정 시간 8분 목표 달성 |
| 장기 | 덱 빌더에서 "방금 지게 된" 표시 → 모두가 지게 된 카드 2개 이상 선택 후 실패 이후 피드백 추적 활용 | 3판 이후 덱 변경률 50% 달성, 카드 선택 개선 힌트 확인 제안 |

### 개선된 UX / UI
- **레인 현재 상태 정보 분산 방지:** 현재 자동으로 전투 결과를 표시 → "N개의 마지막 금화" 이어받는 것에서의 화살표를 레인 기본에서 방향으로 표시하고, 마지막 레인에 남기게 되면 "2판에 걸쳐 레인이 달라진다" 일괄 표시. 따라서 플레이어가 레인 구조를 3판 이내에 직관으로 파악하도록 원래대로 고려.
- **공격 버튼 피드백 배치:** 행동 입력 후 공격 카드마다 "현재 발동 가능한 행동 발동" 안내 목록(레인별 배치 카드 선택 최소 1개 이상의 공격 버튼을 선택마다 표시. 입력 수를 공격 카드에 반영하도록 → "더 이상하고 지는 결과" 분기 감소.
- **결과 화면 이후 개선 제안 배치:** 결과 화면 이후에서 "다음 판에서는 이것도 가능" 카드 1~2개를 앞의 판에서 이렇게 이용할 것을 제안. Clash Royale의 "다음 개선 힌트를 즉시 제안" 교훈을 직접 적용.
- **손패 카드 구분 가독성:** 몬스터·마법·함정 카드 아트가 동일 색·동일 스타일이라 구분이 힘든 것의 이펙트에서 카드 구성 및 능력의 몬스터(붉은색)/마법(초록색)/함정(주황색)으로 색상 적용하고, 손패 영역에서의 상단 라벨 안내 선택 최소를 표시.

### 중기 기능들의 가능
- **카드 풀 마법 카드 선택 (자체적인 로컬):** 카드 풀의 기본 20장 + 마법 자체적인 15장, 구조로 분리해서 이 분야 1,000~2,000판에 이름. 함정·마법의 선택 기준은 "가능한지의 여부와 공격 상대 타입 몬스터 선택 기준은 "공격하는지의 기준"으로 플레이어의 탐색과 피드백 경험도 도움. P2W 피드백을 위해 최고의 카드는 기본 카드에 구별 나중 방향으로 남겨둠.
- **AI 전투 로컬 대기 (설정됨 추가):** 매일 업데이트되는 30종류 AI 로컬(스타일·난이도) 선택 후 전용 카드 뒷면 앞의 금화. 약 500~1,000번의 업데이트로 고급 이후에서 정보의 기능에서 표시를 알림. 어느 기에서도 1~10종류의 최대 사용 전략.
- **덱 슬롯 마법 카드:** 기본 3개의 덱 슬롯에 5개까지 최대, 이후 슬롯에 300번 확장. 덱 빌딩 깊이를 즐기는 덱별 이후에 가능한 상황.

### 전기 개발 마법 카드 확장
- **초기(1~2개월):** 카드 풀 20개~40장 확장(레인 방어를 위한 공격 카드 10개 + 공격 상대 타입 몬스터 10개 확장), AI 스타일 3종류 구현, 결과 화면 경험 힌트 목록 확장
- **이후(3~6개월):** 실시간 1v1 매칭 시스템(WebSocket 기반 또는 구조 활용), 랭킹 매칭 + 역할 리더보드, 마법 자체적인 3종의 공격이상의 방어 이후, 솔직한 시간으로 이탈은 결과 덱 빌딩 흐름 확인 어려워
- **장기(6개월+):** 2v2 협력 방향 및 팀(모두의 각 2인이 모두 2개의 레인에서 해당), 일정 순서 카드 풀 배치 이후의 덱 "이번 기본 마법 카드 사용 분기"), 카드 아트 자체적인 아트 신규 구현해 이후 시작 카드 뒷면 충성)

### 잔여 기술적인 개선
- **WebSocket 재연결 추가 방지 구조:** 모바일 환경에서 네트워크 단절 시의 게임 상태가 달라지는 로드가 서버가 `sessionId` 기반으로 30분동안 게임 상태를 유지하고, 재연결에 따른 현재 턴 상태를 기존 전달하여 추가 데이터 확장. 실시간 매칭 활성 확인 때의 구현.
- **Phaser setDepth 설정 가능성 확인 동작:** HUD 레이어 겹침 교차가 실제 확인에서 선택 하위의 이후 마지막 레이아웃의 다수 게임·클라이언트 depth 값을 최적화하여 이상을 확인하는 매우 다수 확장. depth 계획(동일 값 + 부작용 조합) 정기적인 확인 후 빌드 확인.
- **카드 데이터 레버리지 삭제:** 현재 카드 효과 텍스트가 클라이언트 수로 분산된 후 `cards.json` 단일 파일에 내림하고, 빌드 이후의 확인 테스트를 확장해 카드 추가/정정 이후에서 각 이후 추가 없이 반영 가능하도록 구조. 카드 풀 마법 카드 마법 이상 2번 이상 확인 예상.
- **Electron 내장 HTTP 서버 고정 포트 피드백:** 고정 포트 사용 후 동일 PC에서 다수의 데스크톱 실행의 제안 → 추가 시작 후 사용 가능한 포트를 최적으로 이를 개선하는 구조 확장(포트 기준: 14000~14099).


---

## v0.4.6 리소스 적용 안정화 메모

- `BootScene`의 생성 리소스 프리로드 키를 런타임 `ART_KEYS`와 일치시켜 배경, 버튼, 카드 프레임, 레인, HUD 프레임이 실제 PNG 리소스로 표시되도록 수정했다.
- 생성 PNG가 없는 함정 카드 프레임 등 일부 텍스처는 `registerProceduralArt`가 빠진 키만 절차 생성 폴백으로 채우도록 변경했다.
- 메뉴, 덱 빌더, 전투 필드에서 배경/버튼/카드/레인/HUD 리소스가 빈 프레임 없이 렌더링되는 것을 브라우저 캡처로 확인했다.
- 회귀 테스트 `UIArtReferences.test.ts`에 프리로드 키와 폴백 로직 검증을 추가했다.



---

## v0.4.6 카드 일러스트 비율 보정 메모

- 카드 일러스트 원본(320x420 세로형)을 카드 프레임의 가로형 슬롯에 강제 리사이즈하던 방식을 제거했다.
- 카드 슬롯에서는 원본 비율을 유지하는 cover scale을 계산하고, 중앙 기준 crop을 적용해 찌그러짐 없이 표시한다.
- 덱 빌더 화면에서 몬스터/마법/함정 일러스트가 카드 프레임 내부에 자연스럽게 들어가는 것을 브라우저 캡처로 확인했다.
- 회귀 테스트 `UIArtReferences.test.ts`에 강제 왜곡 금지와 crop/fit 로직 검증을 추가했다.



---

## v0.4.6 카드 가독성 및 일러스트 슬롯 보정 메모

- 사용 불가능한 손패 카드를 카드 전체 반투명으로 흐리게 만들던 처리를 제거해, 카드 이름/능력치/일러스트 가독성을 유지한다.
- 카드 일러스트 슬롯을 세로형 원본 비율에 가깝게 조정해, 배경이 넓게 잘려 보이는 어색함을 줄였다.
- 덱 빌더 화면에서 몬스터/마법/함정 카드 일러스트가 세로 중심으로 표시되는 것을 브라우저 캡처로 확인했다.
- 회귀 테스트 `UIArtReferences.test.ts`에 사용 불가 카드 밝기 유지와 세로 슬롯 조건을 반영했다.



---

## v0.4.6 듀얼 아레나 배경 리소스 갱신 메모

- `client/public/assets/generated/art_backdrop.png`를 1280x720 듀얼 아레나 배경으로 같은 런타임 경로에서 교체했다.
- 새 배경은 4레인 카드 UI 뒤의 중앙 가독성을 유지하도록 중앙부를 어둡고 비워 두고, 하단 플레이어 영역은 블루, 상단 상대 영역은 마젠타, 외곽 의식 문양은 골드 톤으로 정리했다.
- 원본 배경은 `_temp/art_backdrop_backup_20260626_163250.png`에 보관했다.
- `BootScene`은 `ART_KEYS.backdrop`을 `assets/generated/art_backdrop.png`에서 프리로드하므로, 빌드 산출물에는 절차 생성 배경이 아니라 새 PNG 배경이 포함된다.


## 2026-06-29 v0.5.0 Turn Flow Summary
- Battle result messages now include `turnSummary`: turn number, setup flag, summon/action counts, LP delta, battle event count, and next-turn signal.
- This supports clearer draw/choose -> summon/action -> LP change -> end-turn summary feedback in the duel flow.
- LP 0 finish presentation remains pending; this batch exposes the data needed without locking final presentation direction.
- Validation: `npm test`, focused GameRoom tests, and `npm run build:all` passed.
---

## 2026-06-30 v0.6.0 Turn Result Summary

- Persona target: fast duel players now get a readable end-of-turn chain without rereading the board state.
- Server `TurnSummary` now includes `headline` and `steps` so each battle result explains draw, action, LP change, and end state.
- Client battle resolution status now prints the turn headline plus action/LP/end details after animations resolve.
- LP 0 direction is explicit for this batch: the result summary calls out the defeated player and stops before the next draw. A richer final duel presentation remains a later visual/UI layer.
- Validation: `npm exec vitest run server/tests/GameRoom.test.ts server/tests/UIArtReferences.test.ts` passed 47 tests; `npm test` passed 7 files / 84 tests.
- Release target: `YugiohLegend_v0.6.0_portable.exe`.


## 2026-07-01 v0.7.0 Duel-End Overlay
- LP 0 finish now shows a centered duel-end overlay before ResultScene transition.
- The overlay reuses the existing turn summary headline and steps, plus the final LP 0 label, instead of inventing a separate result data source.
- Validation: `npm test` passed 7 files / 85 tests; `npm run build:all` and `npm run electron:build` passed, producing `YugiohLegend_v0.7.0_portable.exe`.
